extends layout

block content
  h1 Hello World
  p Welcome to #{title}

  button(id="heads") Heads
  button(id="tails") Tails
  button(id="flipCoin") Flip Coin!
  button(id="playAgain") Play Again
  input(type="text" id="inputQuery" placeholder="Bet amount $$")
  button(id="placeBet") Place Bet!

  h3

  div(class="image")

  ol
    each user, i in highScores
      li
        = user.name
        = user.score 


  script(src="/javascripts/jquery-2.2.0.js")
  script(src="/javascripts/main.js")